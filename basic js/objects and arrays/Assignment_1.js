//Write a function that takes a user as an input and greets them with their name and age
function greet(userDatabj) {
  console.log(
    "Good Afternoon " + userData.u_name + " and your age is :" + userData.u_age
  );
}
let userData = {
    u_name: "Haris",
    u_age:20
}
greet(userData);